# =====================================================
# VARIÁVEIS DE AMBIENTE - PRODUÇÃO
# =====================================================
# Copie este arquivo para .env na VPS e ajuste os valores
#
# Comando para copiar:
# cp env.production.example .env
#
# IMPORTANTE: NUNCA commite o arquivo .env no Git!

# =====================================================
# SERVIDOR
# =====================================================
# Porta do servidor Socket.IO (deve corresponder ao upstream do Nginx)
PORT=3001

# Ambiente (deve ser "production" em produção)
NODE_ENV=production

# =====================================================
# FRONTEND (CORS)
# =====================================================
# URLs permitidas para CORS (separadas por vírgula se múltiplas)
# IMPORTANTE: Use HTTPS em produção!
FRONTEND_URL=https://www.zkoficial.com.br,https://zkoficial.com.br

# =====================================================
# SUPABASE
# =====================================================
# URL do projeto Supabase (encontre em: Settings > API)
SUPABASE_URL=https://seu-projeto.supabase.co

# Service Role Key (encontre em: Settings > API > service_role key)
# ⚠️ CRÍTICO: Esta chave tem acesso total ao banco (bypass RLS)
# ⚠️ NUNCA exponha esta chave no frontend!
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Anon Key (opcional, usado como fallback se SERVICE_ROLE_KEY não estiver definida)
# Encontre em: Settings > API > anon public key
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# =====================================================
# NOTAS
# =====================================================
# 1. Certifique-se de que o Nginx está configurado corretamente
# 2. Verifique se o certificado SSL está válido
# 3. Teste a conexão WebSocket após configurar tudo
# 4. Monitore os logs: pm2 logs socket-server
# 5. Verifique o health check: https://api.zkoficial.com.br/health
